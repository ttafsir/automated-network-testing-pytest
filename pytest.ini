[pytest]
testpaths = tests

addopts =
    --tb=short
    -v
    -s
    --inventory-path=inventory
    --fabric=DC1_FABRIC
    --showlocals
    # -n 5
    # --html=report.html
    # --self-contained-html
